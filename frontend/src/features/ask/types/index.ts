/**
 * features/ask/types/index.ts
 *
 * Barrel export for all ask feature TypeScript types.
 * Import from this file instead of the concrete module:
 *   import type { AskMessage } from '@/features/ask/types';
 */
export type {
  AskRole,
  AskMessage,
  AskHeaderProps,
  AskSidebarProps,
  AskMessageItemProps,
  AskMessageListProps,
  AskInputFooterProps,
} from './ask.types';
