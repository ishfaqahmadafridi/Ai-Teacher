/**
 * features/intro/types/index.ts
 *
 * Barrel export for all intro feature TypeScript types.
 * Import from this file instead of the concrete module:
 *   import type { Particle } from '@/features/intro/types';
 */
export type { Particle, CategoryItem, SymbolData } from './intro.types';
